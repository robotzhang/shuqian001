<% if @comments.blank? %>
$('#comments').slideDown();
<% else %>
    <% views = @comments[0].commentable.class.to_s.downcase.pluralize; %>
    $('#comments').find('.comments').append($("<%= j render partial: views+"/comments", locals: { comments: @comments } %>"));
    $('#comments').slideDown();
<% end %>