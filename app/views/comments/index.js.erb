var container = $("<%= '#' + render_comment_id(@commentable) %>");
<% views = @commentable.class.to_s.downcase.pluralize; %>
var items = $("<%= j render partial: "comments/items" %>");
var form = $("<%= j render partial: "comments/form" %>");
container.append(items).append(form).slideDown();