<% unless @follow.blank? %>
  var btn = $("<%= j link_to "关注", follow_user_path(@user), method: "post", remote: true, id: "unfollow_"+@user.id.to_s, class: "btn btn-success btn-block" %>");
  $('#follow_<%=@user.id%>').replaceWith(btn);
<% end %>